spring:
  profiles:
    active: development
---
spring:
  profiles: development
  redis:
    host: ${redis.host:192.168.1.63}
    port: 6379
  datasource:
    url: jdbc:mysql://${mysql.host:192.168.1.63}:3306/spring_cloud_event?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root
    initialize: true
eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://${eureka.host:localhost}:8761/eureka/
---
spring:
  profiles: docker
  redis:
    host: redis
    port: 6379
  datasource:
    url: jdbc:mysql://mysql:3306/dev?useSSL=false
    username: root
    password: dbpass
    initialize: true
eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://discovery-service:8761/eureka/
---
spring:
  profiles: test
  redis:
    host: localhost
    port: 6379
  jpa:
    database: H2
  datasource:
    initialize: false